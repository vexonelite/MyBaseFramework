package tw.realtime.project.rtbaseframework.delegates;

public interface AnyIsValidDelegate {
    /**
     * check if the object is valid in order to meet a certain requirement.
     */
    boolean isValid();
}
